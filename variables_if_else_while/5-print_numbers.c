#include <stdio.h>
/**
 * main: starting point
 * description: addiche les chiffre de 0 à 9
 * return: always 0
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");

	return (0);
}
