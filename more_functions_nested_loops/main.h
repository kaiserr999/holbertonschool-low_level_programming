#ifndef MAIN_H
#define MAIN_H

/* --- Tes prototypes de fonctions --- */

/* Exercice 0: isupper */
int _isupper(int c);

/* Exercice 1: isdigit (Si tu l'as déjà fait) */
int _isdigit(int c);

/* Exercice 2: mul (Si tu l'as déjà fait) */
int mul(int a, int b);

/* ... ajoute les autres ici ... */

/* --- Prototype de _putchar (indispensable) --- */
int _putchar(char c);
void print_numbers(void);
void print_most_numbers(void);
void more_numbers(void);

#endif
